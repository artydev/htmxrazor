@page "/products"

<!-- Server-rendered initial load for SEO, then HTMX takes over -->
<div hx-get="/api/products" 
     hx-trigger="load" 
     hx-swap="outerHTML">
    <div class="loading">
        <p>Loading products...</p>
    </div>
</div>
